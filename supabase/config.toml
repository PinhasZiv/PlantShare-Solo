project_id = "plantshare"

# The reminder job is called by pg_cron, which has no user session to present.
# It authenticates with the CRON_SECRET header instead.
[functions.send-reminders]
verify_jwt = false

# The test button is called by a signed-in person, so the JWT is the auth.
[functions.send-test]
verify_jwt = true
